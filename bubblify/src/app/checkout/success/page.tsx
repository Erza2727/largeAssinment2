/** * SuccessPage component displays a confirmation message to the user after a successful order submission.
 * It is the final step in the checkout process and provides feedback to the user that their order has been received and processed.
 */
export default function SuccessPage() {
  return <h1>🎉 Order successful!</h1>;
}
